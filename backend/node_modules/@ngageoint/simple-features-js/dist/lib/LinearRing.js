"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.LinearRing = void 0;
var internal_1 = require("./internal");
/**
 * A LineString that is both closed and simple.
 */
var LinearRing = /** @class */ (function (_super) {
    __extends(LinearRing, _super);
    /**
     * Constructor
     */
    function LinearRing() {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
        }
        var _this = this;
        if (args.length === 0) {
            _this = _super.call(this, args) || this;
        }
        else if (args.length === 1 && args[0] instanceof LinearRing) {
            _this = _super.call(this, args[0].hasZ, args[0].hasM) || this;
            args[0].points.forEach(function (point) { return _this.addPoint(point.copy()); });
        }
        else if (args.length === 1 && args[0].length != null) {
            _this = _super.call(this, internal_1.Geometry.hasZ(args[0]), internal_1.Geometry.hasM(args[0])) || this;
            _this.points = args[0];
        }
        else if (args.length === 2) {
            _this = _super.call(this, args[0], args[1]) || this;
        }
        return _this;
    }
    Object.defineProperty(LinearRing.prototype, "points", {
        /**
         * {@inheritDoc}
         */
        set: function (points) {
            // @ts-ignore
            _super.prototype.points = points;
            if (!this.isEmpty()) {
                if (!this.isClosed()) {
                    this.addPoint(points[0]);
                }
                if (this.numPoints() < 4) {
                    throw new internal_1.SFException("A closed linear ring must have at least four points.");
                }
            }
        },
        enumerable: false,
        configurable: true
    });
    /**
     * {@inheritDoc}
     */
    LinearRing.prototype.copy = function () {
        return new LinearRing(this);
    };
    return LinearRing;
}(internal_1.LineString));
exports.LinearRing = LinearRing;
//# sourceMappingURL=LinearRing.js.map
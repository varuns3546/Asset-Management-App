import { Geometry, Point, LineString } from "./internal";
/**
 * A LineString with exactly 2 Points.
 */
export declare class Line extends LineString {
    constructor();
    constructor(hasZ: boolean, hasM: boolean);
    constructor(points: Array<Point>);
    constructor(line: Line);
    /**
     * Constructor
     *
     * @param point1
     *            first point
     * @param point2
     *            second point
     * @since 1.1.1
     */
    constructor(point1: Point, point2: Point);
    /**
     * {@inheritDoc}
     */
    copy(): Geometry;
}

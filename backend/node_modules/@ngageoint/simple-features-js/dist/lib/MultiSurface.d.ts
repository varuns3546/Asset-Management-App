import { GeometryType, Surface, GeometryCollection } from "./internal";
/**
 * A restricted form of GeometryCollection where each Geometry in the collection
 * must be of type Surface.
 *
 * @param <T> surface type
 */
export declare abstract class MultiSurface<T extends Surface> extends GeometryCollection<T> {
    /**
     * Constructor
     *
     * @param type geometry type
     * @param hasZ has z
     * @param hasM has m
     */
    protected constructor(type: GeometryType, hasZ: boolean, hasM: boolean);
    /**
     * Get the surfaces
     * @return surfaces
     */
    getSurfaces(): Array<T>;
    /**
     * Set the surfaces
     * @param surfaces surfaces
     */
    setSurfaces(surfaces: Array<T>): void;
    /**
     * Add a surface
     * @param surface surface
     */
    addSurface(surface: T): void;
    /**
     * Add surfaces
     *
     * @param surfaces
     *            surfaces
     */
    addSurfaces(surfaces: Array<T>): void;
    /**
     * Get the number of surfaces
     * @return number of surfaces
     */
    numSurfaces(): number;
    /**
     * Returns the Nth surface
     * @param n nth line surface to return
     * @return surface
     */
    getSurface(n: number): T;
}

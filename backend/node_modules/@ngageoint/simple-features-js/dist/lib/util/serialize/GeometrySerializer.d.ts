import { Geometry } from "../../internal";
export declare class GeometrySerializer {
    /**
     * Serialize the geometry to JSON
     * @param geometry geometry
     * @return serialized JSON
     */
    static serialize(geometry: Geometry): string;
    static fromJSON(obj: any): Geometry;
    /**
     * Deserialize the json into a geometry
     * @param json serialized json
     * @return geometry
     */
    static deserialize(json: string): Geometry;
}

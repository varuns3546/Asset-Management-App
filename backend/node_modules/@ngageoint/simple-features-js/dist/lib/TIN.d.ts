import { GeometryType, Geometry, PolyhedralSurface, Polygon } from "./internal";
/**
 * A tetrahedron (4 triangular faces), corner at the origin and each unit
 * coordinate digit.
 */
export declare class TIN extends PolyhedralSurface {
    constructor();
    constructor(polygon: Polygon);
    constructor(tin: TIN);
    constructor(polygons: Array<Polygon>);
    constructor(hasZ: boolean, hasM: boolean);
    constructor(type: GeometryType, hasZ: boolean, hasM: boolean);
    /**
     * {@inheritDoc}
     */
    copy(): Geometry;
}

import { Geometry } from "@ngageoint/simple-features-js";
import { Feature, FeatureCollection, Geometry as gjGeometry, GeometryObject } from "geojson";
/**
 * SimpleFeatures to GeoJSON Geometry Converter
 */
export declare class FeatureConverter {
    /**
     * Converts a GeoJSON Feature to a SimpleFeatures Geometry
     * @param geoJson
     */
    static toSimpleFeaturesGeometryFromGeometryObject(geoJson: GeometryObject): Geometry;
    /**
     * Converts a GeoJSON Feature to a SimpleFeatures Geometry
     * @param geoJson
     */
    static toSimpleFeaturesGeometry(geoJson: Feature): Geometry;
    /**
     * Converts a FeatureCollection into an array of
     * @param geoJson
     * @return Array<Geometry>
     */
    static toSimpleFeaturesGeometryArray(geoJson: FeatureCollection): Array<Geometry>;
    /**
     * Converts a SimpleFeatures Geometry into GeoJSON Geometry
     * @param geometry
     */
    static toFeatureGeometry(geometry: Geometry): gjGeometry;
    /**
     * Converts a SimpleFeature Geometry into a GeoJSON Feature
     * @param geometry
     */
    static toFeature(geometry: Geometry): Feature;
    /**
     * Converts a SimpleFeature Geometry into a GeoJSON Feature Collection
     * @param geometry
     */
    static toFeatureCollection(geometry: Geometry): FeatureCollection;
    /**
     * Converts a GeoJSON geometry into a SimpleFeatures Geometry
     * @param geoJsonGeometry
     * @private
     */
    private static convertFeatureGeometry;
    /**
     * Converts a GeoJSON Position into a Simple Features Point
     * @param position
     * @private
     */
    private static convertFeaturePosition;
    /**
     * Converts a GeoJSON Point feature into a Simple Features Point
     * @param geoJson
     * @private
     */
    private static convertFeaturePoint;
    /**
     * Converts a GeoJSON Position into a Simple Features Point
     * @param positions
     * @private
     */
    private static convertFeaturePositionArray;
    /**
     * Converts a GeoJSON Position into a Simple Features Point
     * @param positions
     * @private
     */
    private static convertArrayOfFeaturePositionArray;
    /**
     * Converts a GeoJSON LineString into a Simple Features LineString
     * @param geoJson
     * @private
     */
    private static convertFeatureLineString;
    /**
     * Converts a GeoJSON Polygon into a Simple Features Polygon
     * @param geoJson
     * @private
     */
    private static convertFeaturePolygon;
    /**
     * Converts a GeoJSON MultiPoint into a Simple Features MultiPoint
     * @param geoJson
     * @private
     */
    private static convertFeatureMultiPoint;
    /**
     * Converts a GeoJSON MultiLineString into a Simple Features MultiLineString
     * @param geoJson
     * @private
     */
    private static convertFeatureMultiLineString;
    /**
     * Converts a GeoJSON MultiPolygon into a Simple Features MultiPolygon
     * @param geoJson
     * @private
     */
    private static convertFeatureMultiPolygon;
    /**
     * Converts a GeoJSON GeometryCollection into a Simple Features GeometryCollection
     * @param geoJson
     * @private
     */
    private static convertFeatureGeometryCollection;
    /**
     * Converts a Point into a position
     * @param geometry
     */
    private static convertPosition;
    /**
     * Convert a Point
     * @return point
     */
    private static convertPoint;
    /**
     * Converts a line string into an array of positions
     * @param geometry
     */
    private static convertPositionArray;
    /**
     * Convert a Line String
     * @return line string
     */
    private static convertLineString;
    /**
     * Converts an array of line strings into an array of array of positions
     * @param lineStrings
     */
    private static convertPositionRings;
    /**
     * Convert a Polygon
     * @return polygon
     */
    private static convertPolygon;
    /**
     * Convert a Multi Point
     * @return multi point
     */
    private static convertMultiPoint;
    /**
     * Convert a Multi Line String
     * @return multi line string
     */
    private static convertMultiLineString;
    /**
     * Convert a Multi Polygon
     * @return multi polygon
     */
    private static convertMultiPolygon;
    /**
     * Read a Geometry Collection
     * @return geometry collection
     */
    private static convertGeometryCollection;
}

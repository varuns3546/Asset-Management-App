import { GeometryType, Geometry, GeometryCollection } from "../internal";
/**
 * Extended Geometry Collection providing abstract geometry collection type
 * support
 *
 * @param <T> geometry type
 */
export declare class ExtendedGeometryCollection<T extends Geometry> extends GeometryCollection<T> {
    /**
     * Extended geometry collection geometry type
     */
    private _editableGeometryType;
    constructor(geometryCollection: GeometryCollection<T>);
    constructor(extendedGeometryCollection: ExtendedGeometryCollection<T>);
    /**
     * Update the extended geometry type based upon the contained geometries
     */
    updateGeometryType(): void;
    /**
     * {@inheritDoc}
     */
    get geometryType(): GeometryType;
    /**
     * {@inheritDoc}
     */
    set geometryType(geometryType: GeometryType);
    /**
     * {@inheritDoc}
     */
    copy(): Geometry;
    /**
     * {@inheritDoc}
     */
    equals(obj: Geometry): boolean;
}

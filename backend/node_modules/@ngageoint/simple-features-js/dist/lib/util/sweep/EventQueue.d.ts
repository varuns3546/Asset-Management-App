import { LineString, Event } from "../../internal";
/**
 * Event queue for processing events
 */
export declare class EventQueue implements Iterable<Event> {
    /**
     * List of events
     */
    private _events;
    constructor(ring: LineString);
    constructor(rings: Array<LineString>);
    /**
     * Add a ring to the event queue
     * @param ring polygon ring
     * @param ringIndex ring index
     */
    private addRing;
    /**
     * Sort the events
     */
    private sort;
    [Symbol.iterator](): Iterator<Event>;
}

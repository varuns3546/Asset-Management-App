"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.EventQueue = void 0;
var internal_1 = require("../../internal");
var TimSort = __importStar(require("timsort"));
/**
 * Event queue for processing events
 */
var EventQueue = /** @class */ (function () {
    function EventQueue() {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
        }
        /**
         * List of events
         */
        this._events = [];
        if (args.length === 1 && args[0] instanceof internal_1.LineString) {
            this.addRing(args[0], 0);
            this.sort();
        }
        else if (args.length === 1) {
            for (var i = 0; i < args[0].length; i++) {
                this.addRing(args[0][i], i);
            }
            this.sort();
        }
    }
    /**
     * Add a ring to the event queue
     * @param ring polygon ring
     * @param ringIndex ring index
     */
    EventQueue.prototype.addRing = function (ring, ringIndex) {
        var points = ring.points;
        for (var i = 0; i < points.length; i++) {
            var point1 = points[i];
            var point2 = points[(i + 1) % points.length];
            var type1 = null;
            var type2 = null;
            if (internal_1.SweepLine.xyOrder(point1, point2) < 0) {
                type1 = internal_1.EventType.LEFT;
                type2 = internal_1.EventType.RIGHT;
            }
            else {
                type1 = internal_1.EventType.RIGHT;
                type2 = internal_1.EventType.LEFT;
            }
            this._events.push(new internal_1.Event(i, ringIndex, point1, type1));
            this._events.push(new internal_1.Event(i, ringIndex, point2, type2));
        }
    };
    /**
     * Sort the events
     */
    EventQueue.prototype.sort = function () {
        TimSort.sort(this._events, function (a, b) { return internal_1.SweepLine.xyOrder(a.point, b.point); });
    };
    EventQueue.prototype[Symbol.iterator] = function () {
        return this._events[Symbol.iterator]();
    };
    return EventQueue;
}());
exports.EventQueue = EventQueue;
//# sourceMappingURL=EventQueue.js.map
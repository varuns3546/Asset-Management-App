"use strict";
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ShamosHoey = void 0;
var internal_1 = require("../../internal");
/**
 * Shamos-Hoey simple polygon detection
 * <p>
 * Based upon C++ implementation:
 * <p>
 * <a href=
 * "http://geomalgorithms.com/a09-_intersect-3.html">http://geomalgorithms.com/a09-_intersect-3.html</a>
 * <p>
 * C++ implementation license:
 * <p>
 * Copyright 2001 softSurfer, 2012 Dan Sunday This code may be freely used and
 * modified for any purpose providing that this copyright notice is included
 * with it. SoftSurfer makes no warranty for this code, and cannot be held
 * liable for any real or imagined damage resulting from its use. Users of this
 * code must verify correctness for their application.
 */
var ShamosHoey = /** @class */ (function () {
    function ShamosHoey() {
    }
    /**
     * Determine if the polygon is simple
     * @param polygon polygon
     * @return true if simple, false if intersects
     */
    ShamosHoey.simplePolygon = function (polygon) {
        return ShamosHoey.simplePolygonLineStrings(polygon.rings);
    };
    /**
     * Determine if the polygon points are simple
     * @param points polygon as points
     * @return true if simple, false if intersects
     */
    ShamosHoey.simplePolygonPoints = function (points) {
        return ShamosHoey.simplePolygonLineString(new internal_1.LineString(points));
    };
    /**
     * Determine if the polygon point rings are simple
     * @param pointRings polygon point rings
     * @return true if simple, false if intersects
     */
    ShamosHoey.simplePolygonRingPoints = function (pointRings) {
        var e_1, _a;
        var rings = [];
        try {
            for (var pointRings_1 = __values(pointRings), pointRings_1_1 = pointRings_1.next(); !pointRings_1_1.done; pointRings_1_1 = pointRings_1.next()) {
                var points = pointRings_1_1.value;
                rings.push(new internal_1.LineString(points));
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (pointRings_1_1 && !pointRings_1_1.done && (_a = pointRings_1.return)) _a.call(pointRings_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return ShamosHoey.simplePolygonLineStrings(rings);
    };
    /**
     * Determine if the polygon line string ring is simple
     * @param ring polygon ring as a line string
     * @return true if simple, false if intersects
     */
    ShamosHoey.simplePolygonLineString = function (ring) {
        var rings = [];
        rings.push(ring);
        return ShamosHoey.simplePolygonLineStrings(rings);
    };
    /**
     * Determine if the polygon rings are simple
     * @param rings polygon rings
     * @return true if simple, false if intersects
     */
    ShamosHoey.simplePolygonLineStrings = function (rings) {
        var e_2, _a;
        var simple = rings.length !== 0;
        var ringCopies = [];
        for (var i = 0; i < rings.length; i++) {
            var ring = rings[i];
            // Copy the ring
            var ringCopy = new internal_1.LineString();
            ringCopy.points = ring.points.slice();
            ringCopies.push(ringCopy);
            // Remove the last point when identical to the first
            var ringCopyPoints = ringCopy.points;
            if (ringCopyPoints.length >= 3) {
                var first = ringCopyPoints[0];
                var last = ringCopyPoints[ringCopyPoints.length - 1];
                if (first.x === last.x && first.y === last.y) {
                    ringCopyPoints.pop();
                }
            }
            // Verify enough ring points
            if (ringCopyPoints.length < 3) {
                simple = false;
                break;
            }
            // Check holes to make sure the first point is in the polygon
            if (i > 0) {
                var firstPoint = ringCopyPoints[0];
                if (!internal_1.GeometryUtils.pointInPolygonRing(firstPoint, rings[0])) {
                    simple = false;
                    break;
                }
                // Make sure the hole first points are not inside of one another
                for (var j = 1; j < i; j++) {
                    var holePoints = rings[j].points;
                    if (internal_1.GeometryUtils.pointInPolygonRingPoints(firstPoint, holePoints) || internal_1.GeometryUtils.pointInPolygonRingPoints(holePoints[0], ringCopyPoints)) {
                        simple = false;
                        break;
                    }
                }
                if (!simple) {
                    break;
                }
            }
        }
        // If valid polygon rings
        if (simple) {
            var eventQueue = new internal_1.EventQueue(ringCopies);
            var sweepLine = new internal_1.SweepLine(ringCopies);
            try {
                for (var eventQueue_1 = __values(eventQueue), eventQueue_1_1 = eventQueue_1.next(); !eventQueue_1_1.done; eventQueue_1_1 = eventQueue_1.next()) {
                    var event_1 = eventQueue_1_1.value;
                    if (event_1.type === internal_1.EventType.LEFT) {
                        var segment = sweepLine.add(event_1);
                        if (sweepLine.intersect(segment, segment.below) || sweepLine.intersect(segment, segment.above)) {
                            simple = false;
                            break;
                        }
                    }
                    else {
                        var segment = sweepLine.find(event_1);
                        if (sweepLine.intersect(segment.above, segment.below)) {
                            simple = false;
                            break;
                        }
                        sweepLine.remove(segment);
                    }
                }
            }
            catch (e_2_1) { e_2 = { error: e_2_1 }; }
            finally {
                try {
                    if (eventQueue_1_1 && !eventQueue_1_1.done && (_a = eventQueue_1.return)) _a.call(eventQueue_1);
                }
                finally { if (e_2) throw e_2.error; }
            }
        }
        return simple;
    };
    return ShamosHoey;
}());
exports.ShamosHoey = ShamosHoey;
//# sourceMappingURL=ShamosHoey.js.map